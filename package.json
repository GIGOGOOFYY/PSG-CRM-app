{
  "name": "psg-crm",
  "version": "1.0.0",
  "private": true,
  "description": "PSG CRM — single-file web app (Supabase + Cloudflare Workers)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
